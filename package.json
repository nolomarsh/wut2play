{
  "name": "pern-test",
  "version": "1.0.0",
  "engines": {
    "node": "14.17.1"
  },
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd api && npm install && cd ../client && npm run build && npm start",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "watch": {
        "start": {
          "patterns": [
            "client/src"
          ],
          "extensions": "js,jsx"
        }
    }
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-watch": "^0.11.0"
  }
}
